This project was created to an simple experimental marketplace.

## Available Scripts

In the project directory, you can run:

### `yarn install`

Installs the app dependencies.<br>

### `yarn start`

Runs the app in the production mode.<br>
